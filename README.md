# Daily Summary: February 25, 2026

## 🚀 Projects & Accomplishments

### 1. Created a Personal Web Page
*   **Goal:** Build a simple, aesthetically pleasing personal landing page with dynamic content.
*   **Implementation:** 
    *   Wrote an `index.html` file using HTML5 and CSS3.
    *   Implemented a modern, dark-mode aesthetic with a "glassmorphism" effect (semi-transparent blur).
    *   Used CSS styling to feature a gradient text effect for your name (`otiyup`).
    *   Implemented JavaScript to display the current local time dynamically, updating every second.
    *   **Visual Preview:**
        ![Personal Page Screenshot](螢幕擷取畫面%202026-02-27%20165044.png)
*   **Location:** Saved locally to `c:\Users\user\Desktop\L1PersonalPage\index.html`.

### 2. Environment Setup & Tooling
*   **Git Installation:** Discovered that Git was not installed on the system.
*   **Package Management:** Used Windows Package Manager (`winget`) to automatically download and install Git (`Git.Git`).

### 3. Version Control & Deployment
*   **Repository Initialization:** Initialized a new local Git repository in the project folder.
*   **Commit History:** Staged the `index.html` file and created an initial commit.
*   **GitHub Integration:** Linked the local repository to your remote GitHub repository (`https://github.com/otiyup/0225DRL`).
*   **Successful Push:** Authenticated with GitHub via the Git Credential Manager and successfully pushed the codebase to the `main` branch.

## 📚 Key Skills & Concepts Applied
*   **Web Development:** HTML document structure, CSS Flexbox, modern CSS text/background effects.
*   **Command Line / Windows:** Using PowerShell and `winget` for software management.
*   **Version Control:** Fundamental Git commands (`git init`, `add`, `commit`, `branch`, `remote`, `push`).
